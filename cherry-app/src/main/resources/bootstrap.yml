eureka:
  client:
    service-url:
      defaultZone: http://localhost:9100/eureka/

spring:
  application:
    name: cherry-app
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: cherry-config-center-server